Mounting : 
    Mounting means putting a React component into the DOM for the first time.
        - When a component is created and inserted into the DOM tree, we say it has mounted.
React Lifecycle (for functional components) :
    React lifecycle has three phases: mounting, updating, and unmounting.
        Mounting →      component is created & rendered for the first time.
        Updating →      when state/props change, component re-renders.
        Unmounting →    when the component is removed from the DOM.
    Note :
        In class components we use lifecycle methods like componentDidMount, componentDidUpdate, and componentWillUnmount. In functional components, the same behavior is handled with useEffect and its cleanup function.

Difference between package.json and package-lock.json : 
        - package.json defines the project metadata, scripts, and dependencies with flexible version ranges.
        - package-lock.json => Automatically generated when you run npm install.Contains the exact versions of every package + nested dependencies.It exact version map of all dependencies.
        - package.json is for humans, package-lock.json is for the machine.
        - package.json is created by Developer (manually) while package-lock.json is auto-generated by npm.

Single Page Application (SPA) : 
    - Single Page Application (SPA) loads a single HTML page and dynamically updates the content on that page using JavaScript without reloading the entire page.
    - Advantage of SPA: 
        - Fast Navigation
        - Better User Experience
        - Reusable component
        - Reduce Server load 

    - Disadvantage of SPA:
        - Initial load time 
        - More JS dependency.

Hooks :
    - Hooks is a normal function which is introduce with functional component. It simulates with life cycle of class based component.
    - Hooks are special functions introduced in React 16.8 that allow functional components to use features like state and lifecycle methods, and other React features without writing class components which were previously available only in class components.
    - Hooks “simulate” lifecycle behavior such as componentDidMount, componentDidUpdate, and componentWillUnmount.
    - It make functional components more powerful and reusable. 
    - It must start with 'use', like useState, useEffect.

Component :
    - Component is reusable piece of code, used in multiple times in different parts of website.
    - A component is a reusable and independent piece of UI in React.
    - Components make code modular, maintainable, and easy to manage.

useState() : 
    - It is used to manage the state of element.
    - useState() is a React Hook used to create and manage state in functional components.
    - It allows you to store dynamic values and re-render the UI whenever the state changes.

useEffect() :
    - I is used to handle the side effect like if any state changes or background some tasks executing which we can't see.
    - useEffect() is a React Hook used to handle side effects in functional components.
    - Side effects include tasks like:
        - Fetching API data
        - Updating DOM manually
        - Setting up subscriptions or timers
    
Babel (Transpiler) :
    - Babel is a JavaScript transpiler that converts modern JavaScript (ES6+ and JSX) into older, browser-compatible JavaScript (ES5).
    - Browsers don’t understand JSX or latest ES features like let, const, arrow functions, async/await, etc. — so Babel transforms them into code that all browsers can execute.

useReducer() :
    - useReducer is a React Hook used to manage complex state logic in functional components — especially when the state depends on multiple sub-values or when the next state depends on the previous one.

Redux Toolkit (RTK) : 
    - Redux Toolkit (RTK) is a set of utilities provided by the Redux team to simplify writing Redux code.
    - It reduces boilerplate by using createSlice for actions + reducers, configureStore for store setup, and createAsyncThunk for API calls.

Throttling : 
    - Throttling limits the rate at which a function can be called, ensuring that it executes at most once within a specified time interval. Unlike debouncing, which waits for a pause, throttling guarantees execution at regular intervals, even if the event continues to fire rapidly.
    - Use Cases in React: 
        - Scroll Event Handlers
        - Mouse Movement Tracking
        - Button Clicks

Debouncing : 
    - Debouncing delays the execution of a function until a certain amount of time has passed without the function being called again. If the function is called again within that time period, the timer is reset.
    - Use Cases in React: 
        - Search Input Fields
        - Form Validation
        - Window Resizing

Difference : 
    - onClick={handleAddItem}
    - onClick={handleAddItem(item)}
    - onClick={()=> handleAddItem(item)}

What are some of the ways to optimize our react code :
    - Use React.memo for Component Re-render Optimization :
        - React.memo() prevents unnecessary re-renders of functional components if their props haven’t changed.
    - Use useMemo and useCallback Hooks
            useMemo → caches computed values.
            useCallback → caches function references.
    - Code Splitting (Lazy Loading)
            Use React.lazy and Suspense to load components only when needed.

    
    - We can use lazy loading
    - Code spliting
    - We can use inbuilt react-hooks like useMemo(), useCallback()

Redux : 
    - Redux is a state management library used to manage global application state in a predictable way.
    - It helps you store and control data (state) in one central place called a store, so components can use that data without passing props everywhere.
    - Redux is needed only when your app becomes large or complex.  

Context API :
    - Context API is a built-in React feature used to share data across components without props drilling.
    - It allows you to create a global state and let any component access it directly.
    - It mainly solves the props drilling problem.
    - Context API is useful for small to medium global state.

Props Drilling Problem :
    - When data needs to be used by many components, you pass props parent → child → grandchild.
    - This becomes painful in large apps.
    - Context API and Redux solves this because Any component can access data directly, no need for props chain.

Note : 
    - Context API and Redux Toolkit do NOT have the same purpose.
    - They look similar because both can share global data, but their purpose, use cases, and capabilities are very different.

Difference between Context API and Redux :
    Context API = ONLY for sharing state globally. It solves props drilling — nothing more.
    Redux Toolkit = full state management system
        It solves:
                How to store state
                How to update state
                How to structure state
                How to handle async logic (API calls)
                How to debug state changes
                How to optimize performance

- Namaste React Interview :
============================
    1. Hooks 
        - useState
        - useEffect
        - useContext
        - useReducer
        - useMemo
        - useCallback
        - useRef
    2. Higher Order component
        - Why we use, how, where and what purpose?
    3. Lifecycle Methods of components
        - class component
            - Mounting
            - Updating
            - Unmounting
    4. State management (All about data)
        - state/props
        - props drilling
        - context
    5. Redux/Zustand
        - Redux Toolkit (RTK)
    6. Custom Hooks 
        - It makes our code maintainable, readable and testable and reusable.
    7. Lazy Loading 
        - Code spliting
        - Chunking 
        - Suspense
    8. Virtual DOM
        - Reconcilation Algorithm
        - React Fiber Algorithm
        - Diff Algorithm
        - How the component render
        - How render works?
    9. SSR(Server Side Rendering) and CSR (Client Side Rendering):
        - SEO (Search Engine Optimization)
        - Performance
    10. Routing (RBAC : Role base Access Controller) :
        - react-router
        - protected routes
        - dynamic routing
    11. Testing : 
        - React Testing library (jest)
    12. Async Tasks 
        - API call
        - Events
        - fetch/Axios
        - promises
        - setTimeInterval()

    13. Reusability/Modularity/Testability
    14. Performance
        - Lazy loading
        - Asset (optimize images)
        - how to optimize js code, css code, how to write better(optimized) code (Bundler level)
        - CDN/Server
        - Rendering the component
    15. Styling :
        - Tailwind css
        - StyleX
        - Bootstrap/Material UI
        - css/external css / internal css
    16. Accessebility
        - Security
        - Performance
        - Accessebility
        - Testing